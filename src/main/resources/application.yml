spring:
  datasource:
    url: jdbc:mysql://localhost:3306/process_db
    username: root
    password: Deusmeu19

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true